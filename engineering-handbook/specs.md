static web project called Engineering Handbook, and is aimed for adding/displaying static markdown as knowledge base or study memo.
using same tech stack with web-tricks. let me know if you want to run npm install or something.
there should be 3 main tags, math, algreba, statistics for now.
each page can belong to multiple tags. and contain 4 parts, the why this matters / real worldpratical implementation, learning goal, learning memo, python implementation. display cleanly in the same page. each supports markdown.
there should be a hidden page only display in localhost for adding/editing a page. and there should also edit or delete option only displayed in localhost.
should support mobile page.
the adding/editing should support 3 markdowns and 1 python edit, and have half side rendered html displayed. if running python on browser cannot be achieved easily, please just support edit and display the python code.
when a page is added, it should create a folder in the project containing the 3 markdowns and a python file. the first pages i want to generate can be referenced from jira-api/create_learning_epic.py. please prefill the other 2 markdown besides leanring memo and python example.
ask question if you have doubts executing.
